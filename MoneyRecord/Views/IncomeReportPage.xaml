<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MoneyRecord.ViewModels"
             xmlns:models="clr-namespace:MoneyRecord.Models"
             xmlns:ext="clr-namespace:MoneyRecord.Extensions"
             xmlns:controls="clr-namespace:MoneyRecord.Controls"
             x:Class="MoneyRecord.Views.IncomeReportPage"
             x:DataType="viewmodels:IncomeReportViewModel"
             Title="{ext:Localize IncomeReport}">

    <Grid>
        <RefreshView IsRefreshing="{Binding IsLoading}"
                     Command="{Binding LoadReportDataCommand}">
            <ScrollView>
                <VerticalStackLayout Padding="16,12" Spacing="16">

                <!-- Period Filter Section -->
                <Frame BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1E1E1E}" 
                       BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#3A3A3A}"
                       CornerRadius="12"
                       Padding="16"
                       HasShadow="True">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="{ext:Localize IncomeAnalysis}" 
                               FontSize="20" 
                               FontAttributes="Bold"
                               HorizontalOptions="Start"/>

                        <Label Text="{ext:Localize IdentifyIncomeCategories}" 
                               FontSize="14"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#AAAAAA}"
                               HorizontalOptions="Start"/>

                        <!-- Period Selector -->
                        <Picker ItemsSource="{Binding Periods}"
                                SelectedItem="{Binding SelectedPeriod}"
                                Title="{ext:Localize SelectPeriod}"
                                FontSize="16"
                                MinimumHeightRequest="44"
                                HorizontalOptions="FillAndExpand"
                                BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#2D2D2D}"/>

                        <!-- Custom Date Range Selector -->
                        <VerticalStackLayout Spacing="12" 
                                            IsVisible="{Binding IsCustomPeriodSelected}">
                            <Grid ColumnDefinitions="Auto,*" 
                                  RowDefinitions="Auto,Auto"
                                  ColumnSpacing="12"
                                  RowSpacing="12">
                                
                                <Label Grid.Row="0" Grid.Column="0"
                                       Text="{ext:Localize From}" 
                                       FontSize="16"
                                       VerticalOptions="Center"
                                       MinimumWidthRequest="50"/>
                                <DatePicker Grid.Row="0" Grid.Column="1"
                                           Date="{Binding CustomStartDate}"
                                           Format="MMM dd, yyyy"
                                           FontSize="16"
                                           MinimumHeightRequest="44"
                                           HorizontalOptions="FillAndExpand"
                                           BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#2D2D2D}"/>
                                
                                <Label Grid.Row="1" Grid.Column="0"
                                       Text="{ext:Localize To}" 
                                       FontSize="16"
                                       VerticalOptions="Center"
                                       MinimumWidthRequest="50"/>
                                <DatePicker Grid.Row="1" Grid.Column="1"
                                           Date="{Binding CustomEndDate}"
                                           Format="MMM dd, yyyy"
                                           FontSize="16"
                                           MinimumHeightRequest="44"
                                           HorizontalOptions="FillAndExpand"
                                           BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#2D2D2D}"/>
                            </Grid>
                        </VerticalStackLayout>

                        <!-- Total Income Display -->
                        <Frame BackgroundColor="{AppThemeBinding Light=#E8F5E9, Dark=#1B5E20}" 
                               BorderColor="{AppThemeBinding Light=#4CAF50, Dark=#66BB6A}"
                               CornerRadius="8"
                               Padding="12"
                               HasShadow="False"
                               IsVisible="{Binding HasData}">
                            <VerticalStackLayout Spacing="4">
                                <Label Text="{ext:Localize TotalIncome}" 
                                       FontSize="14"
                                       TextColor="{AppThemeBinding Light=#2E7D32, Dark=#C8E6C9}"
                                       HorizontalOptions="Center"/>
                                <Label Text="{Binding TotalIncome, StringFormat='${0:N2}'}" 
                                       FontSize="28"
                                       FontAttributes="Bold"
                                       HorizontalOptions="Center"
                                       TextColor="{AppThemeBinding Light=#388E3C, Dark=#66BB6A}"/>
                            </VerticalStackLayout>
                        </Frame>
                    </VerticalStackLayout>
                </Frame>

                <!-- Empty State -->
                <VerticalStackLayout Padding="40" 
                                    Spacing="12" 
                                    HorizontalOptions="Center"
                                    IsVisible="{Binding HasData, Converter={StaticResource InvertedBoolConverter}}">
                    <Label Text="💵" 
                           FontSize="48" 
                           HorizontalOptions="Center"/>
                    <Label Text="{ext:Localize NoIncomeDataFound}" 
                           FontSize="18"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           TextColor="{AppThemeBinding Light=#666666, Dark=#AAAAAA}"/>
                    <Label Text="{ext:Localize AddIncomeTransactionsToSee}" 
                           FontSize="14"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"
                           TextColor="{AppThemeBinding Light=#999999, Dark=#888888}"/>
                </VerticalStackLayout>

                <!-- Heatmap Section -->
                <Frame BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1E1E1E}" 
                       BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#3A3A3A}"
                       CornerRadius="12"
                       Padding="16"
                       HasShadow="True"
                       IsVisible="{Binding HasData}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="{ext:Localize IncomeHeatmap}" 
                               FontSize="18" 
                               FontAttributes="Bold"
                               HorizontalOptions="Start"/>

                        <Label Text="{ext:Localize AllIncomeCategoriesByPercentage}" 
                               FontSize="12"
                               TextColor="{AppThemeBinding Light=#666666, Dark=#AAAAAA}"
                               HorizontalOptions="Start"/>

                        <!-- Heatmap Grid - 3 columns -->
                        <CollectionView ItemsSource="{Binding HeatmapCells}"
                                       SelectionMode="Single"
                                       SelectionChangedCommand="{Binding ShowCategoryDetailsFromHeatmapCommand}"
                                       SelectionChangedCommandParameter="{Binding SelectedItem, Source={RelativeSource Self}}">
                            <CollectionView.ItemsLayout>
                                <GridItemsLayout Orientation="Vertical" 
                                                Span="3"
                                                HorizontalItemSpacing="8"
                                                VerticalItemSpacing="8"/>
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:HeatmapCell">
                                    <Frame BackgroundColor="{Binding BackgroundColor}"
                                           CornerRadius="8"
                                           Padding="8"
                                           HasShadow="False"
                                           HeightRequest="90">
                                        <Frame.GestureRecognizers>
                                            <TapGestureRecognizer 
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:IncomeReportViewModel}}, Path=ShowCategoryDetailsFromHeatmapCommand}"
                                                CommandParameter="{Binding .}"/>
                                        </Frame.GestureRecognizers>
                                        <VerticalStackLayout Spacing="2" 
                                                            VerticalOptions="Center"
                                                            HorizontalOptions="Center">
                                            <Label Text="{Binding DisplayIcon}" 
                                                   FontFamily="MaterialDesignIcons"
                                                   FontSize="22"
                                                   TextColor="{Binding TextColor}"
                                                   HorizontalOptions="Center"/>
                                            <Label Text="{Binding CategoryName}"
                                                   FontSize="11"
                                                   FontAttributes="Bold"
                                                   TextColor="{Binding TextColor}"
                                                   HorizontalOptions="Center"
                                                   HorizontalTextAlignment="Center"
                                                   LineBreakMode="TailTruncation"
                                                   MaxLines="1"/>
                                            <Label Text="{Binding PercentageText}"
                                                   FontSize="14"
                                                   FontAttributes="Bold"
                                                   TextColor="{Binding TextColor}"
                                                   HorizontalOptions="Center"/>
                                        </VerticalStackLayout>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <!-- Legend -->
                        <HorizontalStackLayout Spacing="8" 
                                              HorizontalOptions="Center"
                                              Margin="0,8,0,0">
                            <Frame BackgroundColor="#E8F5E9" 
                                   CornerRadius="4" 
                                   Padding="8,4" 
                                   HasShadow="False">
                                <Label Text="{ext:Localize Low}" FontSize="10" TextColor="Black"/>
                            </Frame>
                            <Label Text="→" 
                                   FontSize="14" 
                                   VerticalOptions="Center"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#AAAAAA}"/>
                            <Frame BackgroundColor="#4CAF50" 
                                   CornerRadius="4" 
                                   Padding="8,4" 
                                   HasShadow="False">
                                <Label Text="{ext:Localize Medium}" FontSize="10" TextColor="White"/>
                            </Frame>
                            <Label Text="→" 
                                   FontSize="14" 
                                   VerticalOptions="Center"
                                   TextColor="{AppThemeBinding Light=#666666, Dark=#AAAAAA}"/>
                            <Frame BackgroundColor="#004D40" 
                                   CornerRadius="4" 
                                   Padding="8,4" 
                                   HasShadow="False">
                                <Label Text="{ext:Localize High}" FontSize="10" TextColor="White"/>
                            </Frame>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Frame>

                <!-- Category Details Section -->
                <Frame BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1E1E1E}" 
                       BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#3A3A3A}"
                       CornerRadius="12"
                       Padding="16"
                       HasShadow="True"
                       IsVisible="{Binding HasData}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="{ext:Localize CategoryDetails}" 
                               FontSize="18" 
                               FontAttributes="Bold"
                               HorizontalOptions="Start"/>

                        <CollectionView ItemsSource="{Binding CategoryDetails}">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:CategoryIncomeData">
                                    <Frame Margin="0,4" 
                                           Padding="12"
                                           BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#404040}"
                                           BackgroundColor="{AppThemeBinding Light=#FAFAFA, Dark=#252525}"
                                           CornerRadius="8"
                                           HasShadow="False">
                                        <Frame.GestureRecognizers>
                                            <TapGestureRecognizer 
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:IncomeReportViewModel}}, Path=ShowCategoryDetailsFromListCommand}"
                                                CommandParameter="{Binding .}"/>
                                        </Frame.GestureRecognizers>
                                        <Grid ColumnDefinitions="Auto,*,Auto,Auto" 
                                              ColumnSpacing="12">
                                            
                                            <!-- Color indicator -->
                                            <BoxView Grid.Column="0"
                                                     WidthRequest="4"
                                                     HeightRequest="40"
                                                     CornerRadius="2"
                                                     BackgroundColor="{Binding HeatmapColor}"
                                                     VerticalOptions="Center"/>

                                            <!-- Category Icon and Name -->
                                            <HorizontalStackLayout Grid.Column="1" 
                                                                  Spacing="8"
                                                                  VerticalOptions="Center">
                                                <Label Text="{Binding DisplayIcon}" 
                                                       FontFamily="MaterialDesignIcons"
                                                       FontSize="20"
                                                       TextColor="{AppThemeBinding Light=#388E3C, Dark=#66BB6A}"
                                                       VerticalOptions="Center"/>
                                                <VerticalStackLayout Spacing="2">
                                                    <Label Text="{Binding CategoryName}"
                                                           FontSize="15"
                                                           FontAttributes="Bold"
                                                           LineBreakMode="TailTruncation"/>
                                                    <HorizontalStackLayout Spacing="4">
                                                        <Label Text="{Binding Amount, StringFormat='${0:N2}'}"
                                                               FontSize="12"
                                                               TextColor="{AppThemeBinding Light=#666666, Dark=#AAAAAA}"/>
                                                        <Label Text="{ext:Localize TapForDetails}"
                                                               FontSize="10"
                                                               TextColor="{AppThemeBinding Light=#999999, Dark=#777777}"/>
                                                    </HorizontalStackLayout>
                                                </VerticalStackLayout>
                                            </HorizontalStackLayout>

                                            <!-- Others Badge -->
                                            <Frame Grid.Column="2"
                                                   BackgroundColor="{AppThemeBinding Light=#E8F5E9, Dark=#1B5E20}"
                                                   BorderColor="Transparent"
                                                   CornerRadius="10"
                                                   Padding="8,4"
                                                   HasShadow="False"
                                                   VerticalOptions="Center"
                                                   IsVisible="{Binding IsOthersGroup}">
                                                <Label Text="&lt;10%" 
                                                       FontSize="10"
                                                       TextColor="{AppThemeBinding Light=#2E7D32, Dark=#A5D6A7}"/>
                                            </Frame>

                                            <!-- Percentage -->
                                            <Label Grid.Column="3"
                                                   Text="{Binding Percentage, StringFormat='{0:F1}%'}"
                                                   FontSize="16"
                                                   FontAttributes="Bold"
                                                   VerticalOptions="Center"
                                                   TextColor="{AppThemeBinding Light=#388E3C, Dark=#66BB6A}"/>
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Frame>

                </VerticalStackLayout>
            </ScrollView>
        </RefreshView>

        <!-- Floating Action Menu -->
        <controls:FloatingActionMenu />
    </Grid>
</ContentPage>
